<h2 class="text-center m-5 p-5 display-4 font-weight-bolder">ユーザー情報編集</h2>
<div class= "conrainer-md row justify-content-center">
  <div class="col-10 col-sm-6">
    <%= form_with model: @user, url: update_user_path, method: :patch do |f| %>
    <table class="table table-borderless">
      <tbody>
      <tr>
        <th><%= f.label :image, "画像" %></th>
        <td><%= f.file_field :image, placeholder: "プロフィール画像", accept: "image/*" %></td>
      </tr>
      <tr>
        <th><%= f.label :nickname, "ニックネーム" %><br /></th>
        <td><%= f.text_field :nickname,  placeholder: "ニックネーム", class: "w-50" %></td>
      </tr>
      <tr>
        <th><%= f.label :introduction, "紹介" %></th>
        <td><%= f.text_area :introduction,  placeholder: "紹介", class: "w-100" %></td>
      </tr>
      </tbody>
    </table>
    <div class="actions text-right p-3">
      <%= f.submit '更新', class: "btn btn-outline-success" %>
    </div>
    <% end %>
  </div>
</div>